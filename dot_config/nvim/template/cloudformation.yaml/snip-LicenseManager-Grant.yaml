  LicenseManagerGrant:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-licensemanager-grant.html
    Type: AWS::LicenseManager::Grant
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-licensemanager-grant.html
      # GrantName: # String
      # LicenseArn: # String
      # HomeRegion: # String
      # AllowedOperations:
      #   - # String
      # Principals:
      #   - # String
      # Status: # String
    # !GetAtt THIS.GrantArn (String)
    # !GetAtt THIS.Version (String)

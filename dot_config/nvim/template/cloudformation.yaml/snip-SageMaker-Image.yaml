  SageMakerImage:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-sagemaker-image.html
    Type: AWS::SageMaker::Image
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-sagemaker-image.html
      ImageName: # String
      ImageRoleArn: # String
      # ImageDisplayName: # String
      # ImageDescription: # String
      # Tags:
      #   - Key: # String
      #     Value: # String
    # !GetAtt THIS.ImageArn (String)

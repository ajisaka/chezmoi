import { ReactNode } from 'react';

type Props = {
  children: ReactNode;
};

const {{_input_:name}} = ({ children }: Props) => {
  return (
    <div>
      {{_cursor_}}
    </div>
  );
};

export default {{_input_:name}};

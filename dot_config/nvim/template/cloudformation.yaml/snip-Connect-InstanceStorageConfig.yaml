  ConnectInstanceStorageConfig:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-connect-instancestorageconfig.html
    Type: AWS::Connect::InstanceStorageConfig
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-connect-instancestorageconfig.html
      InstanceArn: # String
      ResourceType: # String
      StorageType: # String
      # S3Config:
      #   BucketName: # String
      #   BucketPrefix: # String
      #   # EncryptionConfig:
      #   # EncryptionType: # String
      #   # KeyId: # String
      # KinesisVideoStreamConfig:
      #   Prefix: # String
      #   RetentionPeriodHours: # Double
      #   # EncryptionConfig:
      #   # EncryptionType: # String
      #   # KeyId: # String
      # KinesisStreamConfig:
      #   StreamArn: # String
      # KinesisFirehoseConfig:
      #   FirehoseArn: # String
    # !GetAtt THIS.AssociationId (String)

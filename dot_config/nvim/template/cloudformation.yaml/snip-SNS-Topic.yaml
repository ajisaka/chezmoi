  SNSTopic:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-sns-topic.html
    Type: AWS::SNS::Topic
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-sns-topic.html
      # DisplayName: # String
      # KmsMasterKeyId: # String
      # DataProtectionPolicy: # Json
      # Subscription:
      #   - Endpoint: # String
      #     Protocol: # String
      # FifoTopic: # Boolean
      # ContentBasedDeduplication: # Boolean
      # Tags:
      #   - Key: # String
      #     Value: # String
      # TopicName: # String
      # SignatureVersion: # String
    # !GetAtt THIS.TopicName (String)
    # !GetAtt THIS.TopicArn (String)

  BackupBackupVault:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-backup-backupvault.html
    Type: AWS::Backup::BackupVault
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-backup-backupvault.html
      # AccessPolicy: # Json
      BackupVaultName: # String
      # BackupVaultTags:
      #   ANY_KEY: # String
      # EncryptionKeyArn: # String
      # Notifications:
      #   BackupVaultEvents:
      #     - # String
      #   SNSTopicArn: # String
      # LockConfiguration:
      #   MinRetentionDays: # Double
      #   # MaxRetentionDays: # Double
      #   # ChangeableForDays: # Double
    # !GetAtt THIS.BackupVaultName (String)
    # !GetAtt THIS.BackupVaultArn (String)

  WAFRule:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-waf-rule.html
    Type: AWS::WAF::Rule
    Properties:
      MetricName: # String
      Name: # String
      # Predicates:
      #   - DataId: # String
      #     Negated: # Boolean
      #     Type: # String

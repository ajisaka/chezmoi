  NimbleStudioStudio:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-nimblestudio-studio.html
    Type: AWS::NimbleStudio::Studio
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-nimblestudio-studio.html
      AdminRoleArn: # String
      DisplayName: # String
      # StudioEncryptionConfiguration:
      #   KeyType: # String
      #   # KeyArn: # String
      StudioName: # String
      # Tags:
      #   ANY_KEY: # String
      UserRoleArn: # String
    # !GetAtt THIS.HomeRegion (String)
    # !GetAtt THIS.SsoClientId (String)
    # !GetAtt THIS.StudioId (String)
    # !GetAtt THIS.StudioUrl (String)

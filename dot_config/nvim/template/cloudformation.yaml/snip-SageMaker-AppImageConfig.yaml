  SageMakerAppImageConfig:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-sagemaker-appimageconfig.html
    Type: AWS::SageMaker::AppImageConfig
    Properties:
      AppImageConfigName: # String
      # KernelGatewayImageConfig:
      #   # FileSystemConfig:
      #   # # DefaultGid: # Integer
      #   # # DefaultUid: # Integer
      #   # # MountPath: # String
      #   KernelSpecs:
      #     - # DisplayName: # String
      #       Name: # String
      # Tags:
      #   - Key: # String
      #     Value: # String
    # !GetAtt THIS.AppImageConfigArn (String)

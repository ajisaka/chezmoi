  ElasticLoadBalancingLoadBalancer:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-ec2-elb.html
    Type: AWS::ElasticLoadBalancing::LoadBalancer
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-ec2-elb.html
      # AccessLoggingPolicy:
      #   # EmitInterval: # Integer
      #   Enabled: # Boolean
      #   S3BucketName: # String
      #   # S3BucketPrefix: # String
      # AppCookieStickinessPolicy:
      #   - CookieName: # String
      #     PolicyName: # String
      # AvailabilityZones:
      #   - # String
      # ConnectionDrainingPolicy:
      #   Enabled: # Boolean
      #   # Timeout: # Integer
      # ConnectionSettings:
      #   IdleTimeout: # Integer
      # CrossZone: # Boolean
      # HealthCheck:
      #   HealthyThreshold: # String
      #   Interval: # String
      #   Target: # String
      #   Timeout: # String
      #   UnhealthyThreshold: # String
      # Instances:
      #   - # String
      # LBCookieStickinessPolicy:
      #   - # CookieExpirationPeriod: # String
      #     # PolicyName: # String
      Listeners:
        - InstancePort: # String
          # InstanceProtocol: # String
          LoadBalancerPort: # String
          # PolicyNames:
          #   - # String
          Protocol: # String
          # SSLCertificateId: # String
      # LoadBalancerName: # String
      # Policies:
      #   - Attributes:
      #       - # Json
      #     # InstancePorts:
      #     # - # String
      #     # LoadBalancerPorts:
      #     # - # String
      #     PolicyName: # String
      #     PolicyType: # String
      # Scheme: # String
      # SecurityGroups:
      #   - # String
      # Subnets:
      #   - # String
      # Tags:
      #   - Key: # String
      #     Value: # String
    # !GetAtt THIS.CanonicalHostedZoneName (String)
    # !GetAtt THIS.CanonicalHostedZoneNameID (String)
    # !GetAtt THIS.DNSName (String)
    # !GetAtt THIS.SourceSecurityGroup.GroupName (String)
    # !GetAtt THIS.SourceSecurityGroup.OwnerAlias (String)

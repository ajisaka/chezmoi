  SchedulerScheduleGroup:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-scheduler-schedulegroup.html
    Type: AWS::Scheduler::ScheduleGroup
    Properties:
      # Name: # String
      # Tags:
      #   - Key: # String
      #     Value: # String
    # !GetAtt THIS.Arn (String)
    # !GetAtt THIS.CreationDate (String)
    # !GetAtt THIS.LastModificationDate (String)
    # !GetAtt THIS.State (String)

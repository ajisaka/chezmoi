import { ReactNode } from 'react';

type Props = {
  children: ReactNode;
};

const {{_input_:name}} = ({ children }: Props) => (
  <div>
    {{_cursor_}}
  </div>
);

export default {{_input_:name}};

  RedshiftEndpointAccess:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-redshift-endpointaccess.html
    Type: AWS::Redshift::EndpointAccess
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-redshift-endpointaccess.html
      ClusterIdentifier: # String
      # VpcSecurityGroups:
      #   - # VpcSecurityGroupId: # String
      #     # Status: # String
      # ResourceOwner: # String
      EndpointName: # String
      SubnetGroupName: # String
      VpcSecurityGroupIds:
        - # String
      # VpcEndpoint: # Json
    # !GetAtt THIS.Address (String)
    # !GetAtt THIS.EndpointStatus (String)
    # !GetAtt THIS.EndpointCreateTime (String)
    # !GetAtt THIS.Port (Integer)

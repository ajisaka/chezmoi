  AthenaWorkGroup:
    # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-athena-workgroup.html
    Type: AWS::Athena::WorkGroup
    Properties:
      # http://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-athena-workgroup.html
      Name: # String
      # Description: # String
      # Tags:
      #   - Key: # String
      #     Value: # String
      # WorkGroupConfiguration:
      #   # BytesScannedCutoffPerQuery: # Integer
      #   # EnforceWorkGroupConfiguration: # Boolean
      #   # PublishCloudWatchMetricsEnabled: # Boolean
      #   # RequesterPaysEnabled: # Boolean
      #   # ResultConfiguration:
      #   # # EncryptionConfiguration:
      #   #   # EncryptionOption: # String
      #   #   # # KmsKey: # String
      #   # # OutputLocation: # String
      #   # EngineVersion:
      #   # # SelectedEngineVersion: # String
      #   # # EffectiveEngineVersion: # String
      # State: # String
      # RecursiveDeleteOption: # Boolean
    # !GetAtt THIS.WorkGroupConfiguration.EngineVersion.EffectiveEngineVersion (String)
    # !GetAtt THIS.CreationTime (String)
